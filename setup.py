from setuptools import setup

setup(name='gnngls',
      description='build on the project Graph Neural Network Guided Local Search for the Traveling Salesperson Problem',
      packages=['gnngls'],
      )
